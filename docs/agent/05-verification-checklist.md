# 验证清单（完成前必过）

## V1 当前状态（AG-001～AG-005 + Day4 加固）
- 功能、测试、接口、文档已按 Day1～Day4 完成；全量 `pytest -v` 47 通过，CLI 冒烟已跑通。
- 发布说明见 `docs/agent/14-v1-release-note.md`。

---

## 1. 功能验证
- [ ] ingest 正常：`python cli.py --ingest data`
- [ ] query 正常：`python cli.py --query "配送范围？" --debug`
- [ ] 风险问题触发转人工。
- [ ] 回答包含来源或明确无来源。

## 2. 测试验证
- [ ] 新增测试全部通过。
- [ ] 回归测试通过。
- [ ] 全量 `pytest -v` 通过。

## 3. 接口验证（如启用 API）
- [ ] `/health` 返回 200。
- [ ] `/chat` 返回协议字段。
- [ ] `/handoff` 行为符合预期。

## 4. 文档验证
- [ ] README 命令可直接执行。
- [ ] 新增配置项有说明。
- [ ] 任务卡与需求文档状态已同步。

## 5. 提交验证
- [ ] 提交信息语义清晰（feat/fix/test/docs）。
- [ ] 单次提交变更范围可审查。
- [ ] 不包含无关文件改动。
